# Profiles schema

The `profiles` table has two columns:

| Column | SQL type | Constraint |
| --- | --- | --- |
| `id` | `INTEGER` | Primary key |
| `display_name` | `TEXT` | Not null |

This document describes the source schema. It makes no deployment claim.
